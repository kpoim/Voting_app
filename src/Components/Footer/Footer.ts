import styled from 'styled-components';

export const Footer = styled.div`
    margin: auto;
    margin-bottom: 15px;
    display: flex;
    justify-content: space-between;
`;

Footer.defaultProps = {
}
